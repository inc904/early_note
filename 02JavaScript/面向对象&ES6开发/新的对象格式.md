```js
 // 形如：
 [字符串](参数a，参数b){}
 //例如
 let FUNC = 'func'
 const obj = {
		// ES2015 风格的计算属性命名功能来使用一个常量作为函数名
		// 因为原来es5里面属bai性名不能du是变量，只能先定义一个对象，再给对象的属性赋值，es6可以直接定义一个变量做属性名
		[FUNC](a,b){ 
			// code
		}
	}
 // 等价于
 var obj={
 	func:function(a,b){}
 }
 ```